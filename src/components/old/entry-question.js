import React from 'react';
// import './entry-question.css';

export default function EntryQuestion(props) {

    render() {
        return (
            <div>{props.question}</div>
        );
    }
}
